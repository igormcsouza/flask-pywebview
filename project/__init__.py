"""A proof of concept, runing web apps on executables and web applications.

This project aims to create a simple login screen that can be delivered as a
web application or a single file to run in any operational sistem. It is a
simple Flask application, the settings are found under `settings.py` file and
actual app is created on the `starter.py` file with the function create_app().

Typical usage example:

  app = create_app()
  flask.run(app)
"""
